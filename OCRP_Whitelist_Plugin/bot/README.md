# Bot integration

Your existing OCRP V6 bot already has the SA-MP auto-whitelist flow. It sends the configured `SAMP_WHITELIST_COMMAND` over SA-MP RCON.

For this plugin use:

`SAMP_WHITELIST_COMMAND=whitelist add {name}`

No Discord token belongs in the plugin.
